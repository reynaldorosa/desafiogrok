# Nash-loop critique video

The critique iterations are a **code-rendered** recording (exact text, not an
image model). Image-to-video was blocked on this host (ZDR + rate limit),
so the deliverable is a GIF + an HTML player of the same frames.

| file | what |
|---|---|
| [`nash_critique.gif`](nash_critique.gif) | 44-frame replay (~0.9 s/frame, loops) |
| [`index.html`](index.html) | same frames, play/pause, ←/→ |
| `frames/frame_XXX.png` | lossless source |
| `stills/` | cinematic stills (three-player lab, colouring, panels) |

Open `index.html` in a browser, or open the GIF in any image viewer.

Scenes: opening → Grok proposal → GLM critique + unsound LB → DeepSeek
critique + unsound LB → equilibrium → measured bench → verdict 0.97.
